/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configure Next.js to use src/pages instead of the default pages/
  pageExtensions: ['js', 'jsx', 'ts', 'tsx'],
};

module.exports = nextConfig;
